---
description: 该规则用于前后端接口变更追踪与后端任务清单生成
globs: 
alwaysApply: false
---
# API变更追踪与后端任务生成

## 规则目的
此规则用于在前端代码变更时，识别涉及后端接口的变化，并自动生成后端开发任务清单。

## 前端API调用识别
- 识别前端项目中的API调用，主要关注`api`目录中的文件
- 特别注意`api/*.js`文件中的请求路径变更
- 监控`request.js`中的基础URL和请求格式变化

## 任务清单格式
当发现API变更时，在`docs/backend-tasks.md`中生成任务清单，包含以下内容：
1. API路径
2. 请求方法(GET/POST等)
3. 请求参数结构
4. 预期返回结构
5. 功能描述
6. 优先级(High/Medium/Low)
7. 关联的前端模块

## 示例
```markdown
# 后端API开发任务清单

## 1. 新增场馆预约API

- **路径**: `/venue/reserve`
- **方法**: POST
- **请求参数**:
  ```json
  {
    "venueId": 1,
    "date": "2023-06-01",
    "timeSlotId": 2,
    "participants": 2
  }
  ```
- **返回结构**:
  ```json
  {
    "success": true,
    "reservationId": 123
  }
  ```
- **功能**: 用户预约场馆特定时段
- **优先级**: High
- **关联模块**: 场馆预约页面
```

## 执行策略
1. 在前端代码变更时，识别可能影响后端的API变化
2. 分析变更涉及的接口路径、参数和返回值
3. 根据模板生成后端任务描述

4. 将任务保存到docs目录中